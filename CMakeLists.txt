cmake_minimum_required(VERSION 2.8)
project( voronoi_mc )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( voronoi_mc voronoi_mc.cpp )
target_link_libraries( voronoi_mc ${OpenCV_LIBS} )
